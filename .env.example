# 🔐 Ambiente de Produção - FreteAZ Secure API

# Porta padrão do servidor Express
PORT=3000

# URL base do backend principal (mantém a mesma)
BACK_BASE=https://back.clubepostaja.com.br

# Redis (Render) - exemplo de conexão:
# Caso use Render Redis, copie o link completo de conexão em:
# Dashboard → Redis → Connect → URL
REDIS_URL=redis://default:senha@redis-xxxxxx.upstash.io:6379

# Ambiente (opcional)
NODE_ENV=production
